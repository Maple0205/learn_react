This repo used to learn and practice to build some react components by myself.
Such as: customised button, modal, menu bar and etc.

Goal: Dive more into the foundamental implementations of React, get deeper understanding
of how they go.

install npm, run:
  npm install

start npm, run:
  npm start